# HOLOSYN 완전 사용설명서

> 3D 시제품 공간 발표 스튜디오. 만든 사람도 까먹는 버전 — 현재 기준 전체 기능 정리.

---

## 🟢 비기너 3단계 (이것만 알면 됨)

1. **부팅** — 페이지 열면 모달 → `HOLOSYN 엔진 기동` 클릭 (브라우저 오디오 정책상 클릭 1회 필수)
2. **① 넣기** — 왼쪽 `샘플 모델로 시도하기`에서 **Drone / Ring / Car / Core / Exo-Suit** 중 하나 클릭
   - 또는 위쪽 영역에 내 `.glb / .gltf / .obj` 파일이나 이미지를 드래그
3. **② 보기** — `HOLO`(구조) / `COLOR`(실제 색) / `PART`(선택 부품) 중 하나 선택
4. **③ 발표·공유** — 구조·부품·쇼케이스·공유 URL까지 30초 흐름으로 자동 진행

여기까지가 95%. 나머지는 필요할 때만.

---

## 👀 STAGE 관람 모드 (Viewer Mode)

공유 링크를 받은 사람에게는 편집 패널 대신 제품만 크게 보이는 관람 화면이 열립니다.
별도 부팅 없이 장면이 자동으로 시작되며, 링크에 저장된 모델·카메라·조명·재질·부품 상태를
그대로 복원합니다.

- **▶ / Ⅱ** — 자동 발표 재생·정지
- **↶** — 카메라 원위치
- **〈 / 〉** — 이전·다음 부품
- **HOLO / COLOR / PART** — 구조·제품 색상·선택 부품 재질
- **A / B** — 비교 링크에 저장된 두 연출 상태 전환
- **전체화면** — 관람 화면 확장
- **Open Studio** — 같은 장면을 편집 가능한 스튜디오에서 열기

스튜디오의 `SHARE`, `공유 링크 복사`, QR 기능은 Viewer 링크를 기본으로 만듭니다.
직접 관람 모드로 열 때는 주소 뒤에 `?viewer=1`을 붙입니다.

### A/B 장면 비교 링크 만들기

1. 첫 번째 카메라·조명·재질·분해·부품 상태를 만든 뒤 `A/B SCENE COMPARE`에서 A 저장
2. 비교할 두 번째 상태를 만든 뒤 B 저장
3. 눈 아이콘으로 A와 B가 의도대로 복원되는지 미리보기
4. `A/B 비교 링크 복사`를 눌러 관객에게 전달

두 장면은 같은 시제품 모델을 사용해야 합니다. 일반 `공유 링크 복사`는 현재 장면 하나만,
`A/B 비교 링크 복사`는 A와 B 두 장면을 Viewer에 함께 전달합니다.

### 오프라인 전시 모드

- `EXPORT SUITE`의 **전시 링크**를 복사하거나 `HOLOSYN 전시.command`를 더블클릭
- 장면 하나는 시네마틱 발표를 무한 반복
- A/B가 저장돼 있으면 10초마다 A와 B를 자동 교대
- 4.2초 동안 조작이 없으면 화면의 상·하단 도구가 사라지고, 다시 만지면 나타남
- 재생 버튼으로 반복을 일시정지하거나 재개

Three.js·아이콘·QR·폰트가 프로젝트 안에 포함되어 로컬 전시 실행에는 인터넷이 필요하지
않습니다. 다만 실시간 협업·AI 요청은 외부 서버 기능이므로 오프라인에서는 사용할 수 없습니다.
커스텀 모델 전시는 출발 전에 해당 컴퓨터에서 `PORTABLE PROJECT`를 열어 준비합니다.

### 시네마틱 리빌 모드

- 프로 모드 `EXPORT SUITE`의 **리빌 링크**를 복사해 관객에게 전달
- 링크를 열면 현재 장면을 복원한 뒤 18초 공개 연출을 자동 시작
- 암전 → 코어 점등 → 실루엣 → 구조 스캔 → 부품 전개 → 핵심 부품 → 제품 컬러 → 히어로샷 → HOLOSYN 순서
- 진행 중 `SKIP` 또는 `Esc`로 제품 컬러 히어로 상태로 즉시 이동
- 완료 후 상단 반짝임 버튼으로 처음부터 다시 보기
- 완료 뒤에는 일반 Viewer처럼 부품 탐색, HOLO/COLOR/PART, 전체화면 사용 가능

리빌은 첫인상을 위한 1회 연출이고, 전시 모드는 계속 반복되는 무인 화면입니다. 직접 열 때는
주소 뒤에 `?viewer=1&reveal=1`을 붙입니다.

### 실사용 베타 테스트

친구나 동료에게 기능 설명 없이 제품을 써보게 할 때는 주소 뒤에 `?test=1`을 붙이거나,
프로 모드 `BETA OPS PACK`에서 **사용자 테스트**를 누릅니다.

1. 엔진을 켜고 모델 확인
2. `COLOR`로 제품 색상 확인
3. `Part Scan`으로 부품 하나 선택
4. `Exploded`를 40% 이상 올려 구조 확인
5. 공유·전시·리빌 링크 중 하나 생성

과제는 순서와 상관없이 자동 체크됩니다. 끝나면 만족도와 가장 헷갈린 점을 적고
**리포트 JSON**을 누릅니다. 중간에 막힌 경우에도 부분 완료 상태로 저장할 수 있습니다.
리포트에는 모델 파일, 연락처, 전체 URL, 전체 브라우저 정보가 들어가지 않습니다.

---

## 🔒 내 모델은 어디로 가나

**아무 데도 안 갑니다.** 올릴 서버가 없습니다. 파일은 브라우저가 직접 읽어서 화면에
띄우고, 계정도 가입도 없습니다. 개발자도구(F12) → 네트워크 탭을 열고 모델을 넣어보면
직접 확인할 수 있습니다 — 요청이 전부 이 사이트 자기 주소로만 갑니다.

Three.js·아이콘·QR·폰트도 프로젝트 안에 들어 있어서, 랜선을 뽑아도 똑같이 돌아갑니다.
(`HOLOSYN 전시.command`가 그 용도입니다.)

**밖으로 나가는 건 딱 둘입니다.** 둘 다 프로 모드에만 있고, 기본으로 꺼져 있고,
관람·전시·리빌 화면에서는 쓰이지 않습니다.

| 기능 | 어디로 | 언제 |
|---|---|---|
| AI 어시스턴트 | Google Gemini | 본인 API 키를 직접 넣었을 때만 |
| 실시간 협업 | PeerJS 공개 브로커 (연결 중개만) | 세션을 시작했을 때만 |

허용되지 않는 환경이면 두 기능을 켜지 않으면 됩니다. 나머지는 전부 그대로 동작합니다.
사용 기록을 수집하는 코드는 없으며, 발표자 노트·치수·스냅샷은 이 브라우저 안에만
저장됩니다.

---

## 🧭 비기너 / 프로

- **비기너** — `① 넣기 → ② 보기 → ③ 발표·공유`만 표시
- **프로** — 분해·조립, 진단, 타임라인, 측정, 상세 내보내기까지 표시

---

## 🎛️ 왼쪽 패널 (제작·연출)

| 섹션 | 하는 일 |
|---|---|
| **3D 모델 & 이미지 임포트** | 내 파일 드롭 / 컴퓨터에서 열기 |
| **샘플 모델로 시도하기** | 5개 프리셋 |
| **입체 분해도 (Exploded)** | 슬라이더로 부품 분리. 0% / 50% / 100% 스냅 버튼 |
| **로봇 조립 시퀀스** | STEP 1~4 단계별 조립/분해 연출 |
| **AI 진단 / 시제품 점검** | 자동 점검 리포트 |
| **QUICK STYLES** | 미니멀 블루 / 택티컬 실버 / 매트릭스 그린 / **포지 골드**(골드·크림슨 아머랩 HUD) |
| **HOLOGRAM TUNING** *(프로)* | Glow/Rotation/Scanline/Glitch/Scale 슬라이더 |
| **WAVE WAVELENGTH** | 색상 팔레트 |
| **STUDIO SETTINGS** *(프로)* | Sound / Auto Rotate / Spatial / Pyramid 스위치 |

---

## 📐 뷰포트 위 도구 (화면 안 상단)

- **3D 스케치** — 화면에 자유 드로잉 (발표 중 강조)
- **3D 측정** — 모델 표면 점 A→B 클릭하면 거리(mm) 표시
- **더블클릭** — 모델 표면에 콜아웃 핀(주석) 꽂기

---

## 🎥 카메라 도크 (중앙 하단)

`Orbit`(자동 궤도) · `Front`(정면) · `Top`(위) · `Macro`(접사) · `▶ Play Show`(자동 시연) · `Director`(연출) · `Timeline`(키프레임 발표)

---

## 🎤 발표 현장 도구

- **라이브 포인터** — 화면 오른쪽 아래 `포인터` 또는 `Shift+P`. 레이저·자유 드로잉·화살표로 부품을 짚고, 표시는 잠시 뒤 사라짐
- **피치 타이머** — `타이머` 또는 `T`. 30초 / 3분 / 5분 목표와 색상 페이싱 표시
- **QR 띄우기** — `EXPORT SUITE`에서 현재 발표 장면 링크를 QR로 표시. 링크가 너무 길면 QR은 앱 주소로 대체하고, `복사`는 정확한 장면 링크를 유지
- **노트 음성 낭독** — 저장한 발표자 노트를 브라우저 음성으로 읽어 리허설·무인 전시에 사용

---

## 🔍 오른쪽 패널 (사양·검수·내보내기)

| 섹션 | 하는 일 |
|---|---|
| **렌더링 도식화** | Wireframe / Solid / Point Cloud / **열화상** / **엑스레이** |
| **MATERIAL REVEAL** | HOLO=구조, PRODUCT=실제 색·PBR, HYBRID=선택 부품만 실제 재질 |
| **PART SCAN** | 부품을 하나씩(◀▶) 순회하며 강조 + 설명. **발표 핵심 기능** |
| **스튜디오 공간 환경** | 사이버펑크 / 큐퍼티노 / 윈드터널 배경 |
| **세부 사양 설정** | 제품명·분류·질량·출력 등 (직접 편집 가능) |
| **실시간 투사 텔레메트리** | 자동 표시 스펙 |
| **EXPORT SUITE** | 아래 ↓ |

### 내보내기 (EXPORT SUITE + 하단)
- **GLB 다운로드** — 3D 모델 파일
- **사양 JSON** — 스펙 데이터
- **PNG 캡처** — 현재 화면 (사업계획서·SNS용)
- **클라이언트 브리프** — 현재 상태로 발표용 한장 문서(마크다운) 자동 생성
- **데모 팩 / 핸드오프 팩** — 시연 설정 번들
- **QR 띄우기 / 노트 낭독** — 관객 휴대폰 공유와 발표자 노트 음성 재생
- **전시 링크** — 네트워크 없이 자동 반복되는 관객용 전시 화면
- **리빌 링크** — 암전부터 제품 컬러와 히어로샷까지 한 번에 보여주는 관객용 오프닝
- **A/B SCENE COMPARE** — 같은 모델의 두 연출 상태 저장·미리보기·관객용 비교 링크
- **PORTABLE PROJECT** — 실제 모델과 발표 상태를 `.holosyn` 한 파일로 저장·복원

---

## ⌨️ 키보드 단축키

| 키 | 동작 |
|---|---|
| `1` `2` `3` `4` `5` | Wireframe / Solid / Points / 열화상 / X-Ray |
| `←` `→` | (Part Scan 켜짐 시) 이전/다음 부품 |
| `Space` | 회전 멈춤/재생 |
| `E` | 분해도 토글 |
| `R` | 카메라 리셋 |
| `P` | Showcase 모드 |
| `S` | AI 스캔 |
| `F` | 풀스크린 |
| `M` | 사운드 음소거 |
| `L` | 한/영 전환 |
| `Shift+P` | 라이브 포인터 |
| `T` | 피치 타이머 |
| `Esc` | 모달·오버레이 닫기 / 리빌 건너뛰기 |

---

## 📱 모바일 / 태블릿

- 하단 6버튼: **Controls · Render · Scan · Timeline · Sound · Settings**
- 좌상단 햄버거(☰)로 패널 열기
- **터치 제스처(제스처 파일럿)**: 핀치 = 분해도 조절, 드래그 = 카메라

---

## 🟠 비기너 / 프로 모드 (우측 상단)

- **비기너**: 핵심 기능만 (기본값)
- **프로**: HOLOGRAM TUNING, STUDIO SETTINGS, Timeline, 협업, AI, 음성, Pyramid 등 전부 해제
- **🎓 도움말 버튼**: 현재 모드에 맞는 가이드 투어 재시작 (비기너 5스텝 / 프로 5스텝)

---

## 🔮 Pepper's Ghost 실물 시연 (전시·부스)

1. 프로 모드 → STUDIO SETTINGS → **PYRAMID REFLECTION** ON
2. 화면 4분할 → 아크릴 피라미드를 화면 위에 뒤집어 올림
3. 실내 조명 끄면 공중에 입체 홀로그램

---

## 📦 다른 컴퓨터로 프로젝트 옮기기

1. 우측 `PROJECT SNAPSHOT` 아래 `번들 저장` 클릭
2. 생성된 `.holosyn` 파일을 전달
3. 받는 컴퓨터에서 엔진 기동 후 `번들 열기`

모델 GLB, 카메라, 조명, 타임라인, 발표 노트와 저장된 치수가 함께 복원됩니다. 모델 데이터는 최대 80MB까지 번들에 포함되며, 열 때 SHA-256 무결성을 검사합니다.

---

## 🎯 피칭 추천 플로우

1. 샘플 또는 내 `.glb` 파일 선택
2. **PART SCAN** ◀▶ 으로 핵심 부품을 하나씩 설명
3. **3D 측정**으로 중요한 치수를 실측해 강조
4. **MATERIAL REVEAL**로 HOLO(구조) → PRODUCT(실제 색) 전환
5. **타이머**를 30초로 켜고 **포인터**로 핵심 부품 강조
6. **QR 띄우기**로 같은 장면을 관객 휴대폰에 전달
7. **클라이언트 브리프 / PNG** 내보내 사업계획서·심사 핸드아웃

`30s PITCH`는 홀로그램 구조 → HYBRID 부품 포커스 → PRODUCT 컬러 공개 순서로 자동 연출됩니다.

---

## 🥚 숨은 기능

- **로고 트리플 클릭** → 글리치 이스터에그
- **코나미 코드** (`↑↑↓↓←→←→ B A`) → **POWER CORE 모드** (작업실 풀 연출)

---

## ❓ 막힐 때

- **버튼이 안 보임** → 프로 모드인지 확인 (일부는 프로 전용)
- **단일 메쉬 GLB라 분해 안 됨** → 파트 분리된 모델 필요 (안내 토스트 뜸)
- **소리 안 남** → 부팅 클릭 1회 필요 / `M`으로 토글
- **처음부터 다시** → 새로고침
